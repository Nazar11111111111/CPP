@model WebApp.Models.ProfileModel
@{
    ViewData["Title"] = "Profile";
}

@* <div class="container mt-5">
    <div class="card col-md-6 mx-auto border">
        <div class="card-header text-center bg-dark text-white">
            <h3 class="my-1">Personal Info</h3>
        </div>
        <div class="card-body">
            <div class="text-center mb-3">
                <img src="@Model.ProfileImage" alt="Profile Image" style="max-width: 150px;" />
            </div>
            <div class="mt-3">
                <p class="profile-field"><strong>Full Name:</strong> @Model.FullName</p>
                <p class="profile-field"><strong>Username:</strong> @Model.Username</p>
                <p class="profile-field"><strong>Email:</strong> @Model.Email</p>
                <p class="profile-field"><strong>Phone Number:</strong> @Model.PhoneNumber</p>
            </div>
        </div>
    </div>
</div>
 *@

<div class="profile-container">
    <h2 class="profile-title">Personal Info</h2>
    <img src="@Model.ProfileImage" alt="Profile Image" class="profile-image" />
    <div class="profile-items">
        <div class="profile-item">
            <strong>Full Name:</strong> @Model.FullName
        </div>
        <div class="profile-item">
            <strong>Username:</strong> @Model.Username
        </div>
        <div class="profile-item">
            <strong>Email:</strong> @Model.Email
        </div>
        <div class="profile-item">
            <strong>Phone Number:</strong> @Model.PhoneNumber
        </div>
    </div>
</div>



